import reflex as rx

config = rx.Config(
    app_name="link_bio", #Este es el nombre del proyecto, que lo tomó del nombre de la carpeta. Lo podemos cambiar desde acá y cambiando el .py con el nombre dentro de la carpeta link_bio.
)

